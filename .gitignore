# you shouldn't commit these into source control
# these are the default directory names, adjust/add to fit your needs
target/
dbt_packages/
logs/

# local dbt Core connection profile - copy from profiles.yml.example
profiles.yml

# dbt anonymous usage-tracking id, generated locally on first run
.user.yml

# Python
__pycache__/
*.pyc
